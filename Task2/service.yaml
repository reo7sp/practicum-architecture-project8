apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  namespace: default
  labels:
    app: scaletestapp
spec:
  selector:
    app: scaletestapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
